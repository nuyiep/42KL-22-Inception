
#!/bin/sh #specify that the Bourne shell (sh) should be used

if [ -f ./wp-config.php ]
then
        echo "Wordpress has already been installed"
else
        


exec "$@' 
#replaces the shell process with the command-line arguments provided to the script
